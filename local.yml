---

- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: 'Install packages'
    package:
      name:
        - htop
        - vim
        - tmux

  - name: 'Copy .vimrc'
    copy:
      src: dot_files/.vimrc
      dest: /home/pshnn/.vimrc
      owner: pshnn
      group: pshnn
